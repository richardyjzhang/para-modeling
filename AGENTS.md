# para-modeling — AI 协作约定

本项目是一个 **Web 端通用参数化建模工具**，借鉴 AVEVA PDMS 的元件库（Catalogue）机制，面向桥梁、房建等工程场景，主要输出三维可视化。

**任何会话开工前，必须先阅读：**

- `docs/design.md` — 总体设计与决策记录（权威版本，设计冲突以它为准）
- `docs/conventions.md` — 数据约定与词汇表（所有枚举值/字段名/拼写的唯一登记处，新增词汇先登记再写代码）
- `docs/primitives.md` — 内置图元几何约定（参数含义、局部坐标系；改图元时必读）
- `docs/roadmap.md` — 20 期开发路线图与当前进度

## 技术栈（已定，除非人工要求，否则勿更换）

| 端 | 技术 |
|---|---|
| 前端 | Vue 3 + TypeScript + Vite + Three.js + Tailwind CSS + math.js |
| 后端 | Python + Flask + SQLite |
| 通信 | REST API，JSON |

## 目录结构

```
para-modeling/
├── AGENTS.md            # 本文件
├── docs/                # 设计文档、路线图、图元约定、schema.sql
├── frontend/            # Vue3 前端
│   └── src/
│       ├── api/         # 后端接口封装
│       ├── components/  # Vue 组件
│       ├── core/        # 纯逻辑：表达式求值、模板展开、几何构建（不依赖 Vue）
│       ├── stores/      # Pinia 状态
│       └── views/       # 页面
└── backend/             # Flask 后端
    ├── run.py           # 入口
    ├── db/              # 建表 SQL / 迁移
    ├── models/          # 数据访问
    └── api/             # 路由（Blueprint）
```

## 架构红线（违反 = 返工）

1. **几何只在前端算。** 后端只存参数化描述（模板 JSON、参数值），从不生成/存储网格与坐标计算结果。渲染管线：模板 + 参数 → 表达式求值 → 展开为纯图元树 → Three.js 几何。
2. **尺寸字段一律按表达式字符串存**（如 `"height": "300"`），即使当前 UI 只允许填数字。禁止把尺寸存成 number 类型，避免参数化阶段返工。
3. **模型树节点支持两种类型**：裸图元（`primitive`）与元件实例（`instance`），另有分组节点（`group`）。数据结构从第一天起就要兼容三者。
4. **元件模板内部定义整体存 JSON**；但**参数列表**与**模板间依赖关系**冗余拆表，供列表页与循环检测使用。工程模型层级树用结构化邻接表存储。具体表结构以 `docs/schema.sql` 为准（随分期实现逐渐补充）。
5. **表达式两端校验一致。** 前端 math.js 解析 + AST 白名单（数字、变量、`+ - * / ^`、白名单函数）；后端 Python `ast.parse` + 相同白名单，只校验不求值。白名单函数与语法约定见 `docs/design.md`，两端修改必须同步。
6. **元件嵌套规则**：子模板参数只能通过 `bindings` 传入；表达式只能引用本模板自己的参数；禁止循环引用（保存时后端依赖图检查）；引用始终指向最新版本（活引用）。
7. `frontend/src/core/` 下是纯 TypeScript 逻辑，**不得 import Vue / Pinia / DOM API**，保证可单测、可复用。

## 工作流约定

- **分期开发**：严格按 `docs/roadmap.md` 的分期走，每次只做一期，不提前实现后续期的功能（数据结构预埋除外，见红线 2、3）。
- **每期收尾**：更新 `roadmap.md` 的进度标记；若有设计变更，先改 `docs/design.md` 再动代码。
- **代码可读性优先**：用户要逐行读懂代码。宁可直白冗长，不要炫技抽象；关键逻辑写中文注释说明"为什么"。
- 早期阶段允许简陋交互（如原生弹窗），后续期迭代替换，不必一步到位。
- 暂不考虑：三维性能优化（InstancedMesh/LOD）、多用户权限、PDMS 的 Spec 等级库、模板版本固定。

## 语言约定

- 与用户交流、代码注释、文档：**简体中文**
- 代码标识符、API 路径、数据库字段：英文
